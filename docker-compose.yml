services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: jump
      POSTGRES_USER: jump
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - jump-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jump"]
      interval: 5s
      timeout: 5s
      retries: 5

  sveltekit:
    build:
      context: frontend
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - jump-network
    environment:
      - PORT=${PORT}
      - ORIGIN=${ORIGIN}
      - DATABASE_URL=postgresql://jump:${POSTGRES_PASSWORD}@postgres:5432/jump
      - HOST_HEADER=x-forwarded-host
    restart: unless-stopped

volumes:
  pg_data:

networks:
  jump-network:
    driver: bridge
